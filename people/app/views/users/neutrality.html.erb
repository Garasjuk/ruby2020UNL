<article class="people">
    <section class="people-head">
      <div class="container">
        <h2>Main page</h2>

            <%= will_paginate @neutrality, :renderer => 'BootstrapPagination::Rails' %>

          <div class="row">
            <% @neutrality.each do |neutrality| %>
                    <div class="gallery_product col-lg-4 col-md-4 col-sm-4 col-xs-6 filter hdpe">
                      <h2><%= link_to neutrality.name, neutrality_path(neutrality) %></h2>
                      <br>
                      <small>
                        Type: <b><%= neutrality.type %></b> <br>
                        <%= link_to 'Edit', edit_user_path(neutrality), class: 'btn btn-warning' %>
                        <%= link_to 'Destroy', user_path(neutrality), method: :delete, data:{confirm: 'Are you sure?'}, class: 'btn btn-danger' %><br>
                      </small>
                      <hr>
                    </div>
            <% end %>
          </div>

        <%= will_paginate @neutrality, :renderer => 'BootstrapPagination::Rails' %>
        </div>
    </section>
</article>
